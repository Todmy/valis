version: "3.8"

# Valis Community Edition — Self-hosted
# Usage: docker compose up -d
# Then:  valis init (choose Community, enter localhost URLs)

services:
  # PostgreSQL with auto-applied migrations
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: valis
      POSTGRES_USER: valis
      POSTGRES_PASSWORD: valis_local_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U valis"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Qdrant vector search
  qdrant:
    image: qdrant/qdrant:v1.12.1
    restart: unless-stopped
    ports:
      - "6333:6333"    # REST API
      - "6334:6334"    # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:6333/healthz || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  qdrant_data:
